{
  "name": "json-schema-tools",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "validate:schemas": "ajv compile -s 'schemas/**/*.json'",
    "check": "npm run validate:schemas && echo '✅ All JSON schemas are valid!'"
  },
  "devDependencies": {
    "ajv-cli": "^5.0.0"
  }
}